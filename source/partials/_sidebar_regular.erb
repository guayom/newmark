<h4><%= title %></h4>
<ul id="navID" class="navlist">
  <% sub_pages(collection).each do |page| %>
    <li>
      <% if page.data.title == current_page.data.title %>
        <a href="#" disabled tabindex="-1"><%= page.data.title %></a>
      <% else %>
        <a href="<%= page.url %>"><%= page.data.title %></a>
      <% end %>
    </li>
  <% end %>
</ul>
<select onChange="window.location.href=this.value" class="subnav-mobile-navigation">
  <% sub_pages(collection).each do |page| %>
    <option value="<%= page.url %>"><%= page.data.title %></option>
  <% end %>
</select>
